# Run 41 — MX2 structure queue preregistration

The structure queue is selected only by cohort role and the fixed stability
rule. Band Gap is neither read nor ranked during queue construction.

The expected queue contains 25 records from MoSe2, MoTe2, WS2 and WSe2. WTe2
currently contributes no record because none passed the already-fixed combined
stability prefilter. MoS2 remains development-only.

For the frozen MP IDs, the official API request is limited to identity,
formula, structure and Summary origins. Returned records remain quarantined.
Larsen/Gorai dimensionality evidence and calculation-method evidence are
separate subsequent gates; neither the API response nor an algorithm may
self-approve a record.
