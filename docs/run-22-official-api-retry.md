# 第 22 次运行：官方 API 页面确认与正式管线重试

## 1. 本次完成了什么

- 在已登录的 Materials Project 官方 API 页面确认 API 用途、推荐客户端、数据库版本与许可证。
- 未记录或输出 API 密钥值；公开审计只记录“页面可见”和“凭据未保存”。
- 再次运行 `scripts/run_formal_mp_pipeline.py`。
- 官方 `mp-api` 客户端当前环境未安装，REST 回退仍在 DNS 解析阶段失败。
- 正式管线按设计停止：未映射、未晋级、未启动正式闭环，也未以随机数据或网页抄录替代 API 数据。

## 2. 对原有设计的落实与调整

- 落实 Materials Project 单一正式数据源政策。
- 落实 API 响应必须有成功/失败提示和机器可读审计记录。
- 落实“网页索引证据不等于 API 响应”：页面信息只用于来源、版本和入口确认。
- 落实失败关闭：获取失败后，后续映射与闭环不得继续。
- 新增账号状态提示：页面仍显示注册完成表单，因此当前记录不独立宣称账号资料流程已经最终提交；这不等同于判定 API 密钥无效。

## 3. 对应初赛文稿哪些部分

- §2.1：固定 Materials Project 来源、公式查询范围、请求字段和数据库版本记录规则。
- §2.2：API 获取是正式环境反馈入口；失败时返回结构化错误而不是伪造标签。
- §2.3：请求时间、查询、客户端路径、返回数量、失败类型与下一闸门均写入审计。
- §3.3：没有成功 API 响应即判定正式队列失败，不允许声明正式发现。
- §4.1：正式试跑入口已经执行，并产生可检查的阻断结果。
- §4.2：依赖不可用和 DNS 失败是两条独立失败路径。
- §4.3：官方客户端、版本、许可证和本地秘密隔离边界得到记录。

## 4. 接下来讨论、设计与落地什么

1. 用户在 Materials Project 页面确认注册完成表单是否已保存；若仍显示，可自行勾选条款并保存。
2. 在可联网的本机终端安装固定版本 `mp-api`，然后运行正式管线。
3. 成功后核对返回记录数、字段缺失和 Materials Explorer 的 12 条索引交叉匹配。
4. 执行二维性和计算方法人工审核；合格记录达到 8 条才进入正式闭环。

