# 第 25 次运行：API 传输诊断与官方客户端安装入口

## 1. 本次完成了什么

- 确认本地秘密文件存在且正式管线能够读取凭据；未读取或显示密钥值。
- 正式管线再次执行，官方客户端路径因 `mp_api` 尚未安装而不可用。
- REST 回退已越过此前的 DNS 故障，但在本机 Python 的 SSL 证书校验处失败。
- 因无成功响应，返回 0 条记录，映射和人工审核队列未启动。
- 新增 `scripts/install_mp_api_and_run.command`，在项目隔离环境中安装固定版本官方客户端后自动重跑。

## 2. 对原有设计的落实与调整

- 保持官方 `mp-api==0.46.4` 为首选路径。
- 不采用关闭 SSL 校验等不安全规避方法。
- 安装范围限定于被 Git 忽略的 `.venv`，不污染系统 Python，也不进入公开包。
- 正式获取失败后仍然禁止网页数据和随机数据替代。

## 3. 对应初赛文稿哪些部分

- §2.1：依赖版本、数据源和请求字段固定。
- §2.2：正式反馈接口实际返回传输失败状态。
- §2.3：客户端缺失与 SSL 失败被分别记录。
- §3.3：0 条 API 记录即正式队列失败。
- §4.1：完成一次正式入口试跑。
- §4.2：定位 DNS、依赖和证书链为不同失败阶段。
- §4.3：采用隔离环境与固定依赖版本提高复现性。

## 4. 接下来讨论、设计与落地什么

1. 在本机终端完成官方客户端安装。
2. 自动重跑并检查是否返回 MoS2 记录。
3. 若成功，执行映射、索引交叉核对和证据护照更新。
4. 若客户端仍失败，保留其明确错误，不回退到不安全 SSL 方案。

