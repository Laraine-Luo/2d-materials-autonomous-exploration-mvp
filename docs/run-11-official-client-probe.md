# 第11次运行：官方mp-api优先探针

## 1. 本次完成了什么

将正式数据探针改为官方`mp-api==0.46.4`优先、REST降级。审计文件分别记录客户端是否安装、连接是否成功以及降级结果，不再只保留最后一个模糊错误。

实际安装尝试同样在域名解析阶段失败，已保存`artifacts/dependency_install_audit.json`。因此当前阻塞被定位为执行环境网络/DNS，而不是数据映射、API密钥或MoS2查询语法。

## 2. 对原有设计的落实与调整

不改变MP单一数据源、MoS2首队列、简单评价函数或三策略。新增官方客户端版本固定，是为了符合可复现要求；REST只作为连接诊断降级路径，不产生第二数据源。

## 3. 对应初赛文稿

支撑§2.1依赖版本固定、§2.3响应审计、§4.1真实数据入口、§4.2客户端/网络失败分层和§4.3依赖复现。

## 4. 接下来讨论、设计与落地

安装固定版本客户端后再次执行；成功时核验12条MoS2记录和字段，失败时根据分层审计定位为安装、DNS、认证、HTTP或字段错误。

新增`run_formal_mp_pipeline.py`作为唯一正式入口：采集失败即停止映射，且明确记录“未使用合成或网页转录数据替代”。该状态可以直接作为报告中的一次失败路径证据。
