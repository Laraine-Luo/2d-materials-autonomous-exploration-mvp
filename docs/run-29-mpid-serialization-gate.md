# 第 29 次运行：MPID 序列化闸门修正

## 1. 本次完成了什么

- 读取失败审计，确认请求成功返回 12 条，但身份集合硬闸门阻止了映射。
- 定位到 `MPID` 字符串子类的双重表示：直接访问得到公开数值 ID，通用 JSON 序列化写出内部字母值。
- 新增显式序列化函数，规范化 `material_id` 与 `origins.task_id`。
- 新增回归测试，模拟“底层字母值/公开数值值”并确保只保存公开身份。

## 2. 对原有设计的落实与调整

- 身份闸门没有被放宽；修正的是序列化，不是评价标准。
- 请求与返回 ID 仍须完全相同。
- `origins.task_id` 同步规范化，为后续计算方法联查保证主键一致。
- 失败结果继续保留，证明审计机制能够阻止表示层错误污染正式数据。

## 3. 对应初赛文稿哪些部分

- §2.2：反馈数据先经过公开身份规范化。
- §2.3：请求—返回 ID 一致性进入日志。
- §3.1：接口表示异常属于可解释失败模式。
- §3.3：错误身份表示触发明确失败。
- §4.2：记录序列化层导致身份污染的风险和修复。
- §4.3：回归测试保证后续运行不重复出现同类错误。

## 4. 接下来讨论、设计与落地什么

1. 重跑正式管线并验证 12/12 公开 ID 精确匹配。
2. 更新证据护照。
3. 通过规范化后的 `origins.task_id` 联查 thermo 计算方法。
4. 二维性仍由人工审核确认。

