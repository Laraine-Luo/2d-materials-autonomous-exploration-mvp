# 第14次运行：仓库索引展示页与mp-1434证据护照

## 1. 本次完成了什么

新增由CSV自动生成的MoS2仓库索引展示页，包含12个MP ID及网页表格性质；新增mp-1434证据护照，将网页索引、网页详情、官方API、人工审核和探索状态分层记录。

## 2. 对原有设计的落实与调整

索引清单现在既保留机器可读CSV，也提供评委可直接阅读的Markdown。CSV是唯一事实源，展示页自动生成，避免README/PDF手工抄写漂移。mp-1434仍为D0、不可训练，网页详情中的R2SCAN只记为“页面显示”，未经`origins → thermo run_type`联查不通过方法审核。

## 3. 对应初赛文稿

- §2.1：清楚呈现公式层与MP ID候选层；
- §2.3：逐材料证据护照和审核状态可追踪；
- §3.1：展示数值命中与正式发现资格的区别；
- §4.1：mp-1434作为一次试跑的输入样例；
- §4.2：每个待确认字段对应一个显式阻塞原因；
- §4.3：CSV、生成脚本、Markdown和JSON均进入仓库。

## 4. 接下来讨论、设计与落地

将API成功响应写入mp-1434护照，完成第一条晋级判定；然后自动批量生成其余11条护照，并基于审核结果决定是否达到8条最小闭环门槛。PDF可直接截取索引表、护照状态和索引路径。
